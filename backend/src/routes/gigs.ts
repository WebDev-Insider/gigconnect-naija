import { Router } from 'express';

const router = Router();

// TODO: Implement gig routes
// GET / - List gigs with filters
// POST / - Create new gig
// GET /:id - Get gig details
// PUT /:id - Update gig
// DELETE /:id - Delete gig

export default router;
